services:
  pm_gw_dashboard:
    build:
      context: ../dashboard
    container_name: pm_gw_dashboard
    ports:
      - "4173:80"
    restart: unless-stopped
  pm_gw_trade:
    build:
      context: ../trade_app
    container_name: pm_gw_trade
    ports:
      - "4174:80"
    restart: unless-stopped
